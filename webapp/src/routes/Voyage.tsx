/** Voyage screen - automatically detected voyages. Layout provides header + tab bar. */
import VoyageMarine from "siparu-ui/screens/voyage";

export default function Voyage() {
  return <VoyageMarine />;
}
