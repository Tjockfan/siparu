/** Logbook screen - single surface. Data/logic lives in logbook/useLogbookData.ts.
 *  Legacy theme dispatcher removed (DESIGN-SYSTEM.md Phase 5). */
import LogbookMarine from "./logbook/LogbookMarine";

export default function Logbook() {
  return <LogbookMarine />;
}
